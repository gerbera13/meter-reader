# План: превью фото для каждого счётчика

## Задача
Под каждым блоком счётчика показывать фото в ряд, одного размера.
Количество фото = количество заполненных тарифов.

## Как работает сейчас (проблема)
- Фото добавляются при каждой загрузке панели всем 4 счётчикам
- Размер разный (было 100/82/64)
- Не привязаны к конкретному тарифу

## Как должно работать

### Структура данных
Каждый тариф (T1/T2/T3) хранит своё фото:
```
m.readings["T1"] = { value: "32548", date: "...", photo: "data:image/jpeg;..." }
m.readings["T2"] = { value: "09215", date: "...", photo: "data:image/jpeg;..." }
```

### Алгоритм
1. При загрузке панели — для каждого заполненного тарифа сохраняем фото
2. При рендеринге — показываем фото только для тарифов, где есть photo
3. Все фото одного размера (100px), в ряд

### Визуализация

```
┌─ ул.Садовая д.10 ────────┐
│ T1 [32548] 📷 пред:+148  │
└──────────────────────────┘
  [фото_T1]

┌─ ул.Рябиновая д.22 ──────┐
│ T1 [18903] 📷 пред:+153  │
│ T2 [09215] 📷 пред:+165  │
└──────────────────────────┘
  [фото_T1] [фото_T2]

┌─ ул.Парковая д.5 ────────┐
│ T1 [45678] 📷 пред:+178  │
└──────────────────────────┘
  [фото_T1]

┌─ ул.Лесная д.8 ──────────┐
│ T1 [12340] 📷 пред:+160  │
│ T2 [06712] 📷 пред:+132  │
│ T3 [29876] 📷 пред:+176  │
└──────────────────────────┘
  [фото_T1] [фото_T2] [фото_T3]
```

## Что изменить в коде

### 1. makeEmpty / loadState
Добавить поле `photo` в каждый тариф:
```js
readings["T1"] = { value: "", prev: "", date: "", photo: "" }
```

### 2. saveState — без изменений
Фото уже внутри readings, сохраняются автоматически.

### 3. Панельный обработчик
При заполнении тарифа — сохраняем фото в этот тариф:
```js
m.readings[slot.tariff].photo = croppedDataUrl;
```

### 4. renderMeters
Показываем фото только для заполненных тарифов:
```js
const photos = [];
for (let i = 1; i <= m.tariffs; i++) {
  const p = m.readings["T"+i].photo;
  if (p) photos.push(p);
}
// рендерим photos в meter-preview, все 100px
```

### 5. Сброс
Очищать photo вместе с value/date.

### 6. Индивидуальное фото (кнопка 📷 у тарифа)
Сохранять фото в конкретный тариф, не в общий массив _previews.
